# Security: Disable install scripts to prevent supply chain attacks
# Packages with legitimate install scripts can be allowlisted individually
ignore-scripts=true

# Use lockfile for reproducible builds
package-lock=true
